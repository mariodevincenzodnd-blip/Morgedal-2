#:schema https://developers.openai.com/codex/config-schema.json

# Morgedal 2 project configuration.
# Keep the standard service tier to avoid unnecessary Fast-mode consumption.
# Reasoning effort is intentionally not forced here: the active Codex/Astra
# session remains free to use the level appropriate for the task.
service_tier = "default"
